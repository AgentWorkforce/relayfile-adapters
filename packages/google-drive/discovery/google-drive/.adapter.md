# Google Drive adapter

The Google Drive adapter exposes files and watch channels with file-native writeback discovery.

Read-only mounts:
- `/google-drive/<account>/<path>` - Drive file content.

Resources:

| Resource | Schema | Create example | ID pattern | What it does |
|---|---|---|---|---|
| `/google-drive/files/<id>.json` | `/google-drive/files/.schema.json` | `/google-drive/files/.create.example.json` | `^[A-Za-z0-9_.:-]+$` | Creates a Drive file. |
| `/google-drive/channels/<id>.json` | `/google-drive/channels/.schema.json` | `/google-drive/channels/.create.example.json` | `^[A-Za-z0-9_.:-]+$` | Creates a Drive watch channel. |

## Operations

| To... | Do... |
|---|---|
| Read | `cat <id>.json` |
| Edit | Write a partial JSON object to `<id>.json`. Only included mutable fields PATCH; fields marked `readOnly` in `.schema.json` are rejected. |
| Create | Write JSON to any non-canonical filename such as `create request.json`. The adapter creates the record at `<real-id>.json` and rewrites the draft as `{ "created": "<real-id>", "path": "<resource>/<real-id>.json", "url": "<provider-url>" }`. |
| Delete | `rm <id>.json` for canonical ids. |

## ID Patterns
- `/google-drive/files/<id>.json`: `^[A-Za-z0-9_.:-]+$`. Filenames that do not match this pattern are treated as create drafts.
- `/google-drive/channels/<id>.json`: `^[A-Za-z0-9_.:-]+$`. Filenames that do not match this pattern are treated as create drafts.

## Write field contracts

### Create Google Drive file

Resource: `/google-drive/files/<id>.json`
Schema: `/google-drive/files/.schema.json`
Create example: `/google-drive/files/.create.example.json`
Required fields: `name`.
Optional fields: `mimeType`, `parents`.

Fields:

- `name` (required, string) - File name.
- `mimeType` (optional, string) - MIME type.
- `parents` (optional, array) - Parents.

### Create Google Drive watch channel

Resource: `/google-drive/channels/<id>.json`
Schema: `/google-drive/channels/.schema.json`
Create example: `/google-drive/channels/.create.example.json`
Required fields: `resourceId`, `address`.
Optional fields: `type`.

Fields:

- `resourceId` (required, string) - Resource id.
- `address` (required, string) - Webhook address.
- `type` (optional, string) - Channel type.

## Create Examples
Read the resource `.schema.json` first, then use the sibling `.create.example.json` as a minimal create document. The example intentionally omits read-only fields.
