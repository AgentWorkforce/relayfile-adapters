# Teams adapter

The Teams adapter exposes teams, channels, messages, replies, chats, tabs, members, and reactions under `/teams`, with writeback routes for creating channel messages, replies, and chat messages.

Read-only mounts:
- `/teams/<teamId>/metadata.json` - Team records.
- `/teams/<teamId>/channels/<channelId>/metadata.json` - Channel records.
- `/teams/<teamId>/channels/<channelId>/messages/<messageId>.json` - Channel message records.
- `/teams/chats/<chatId>/messages/<messageId>.json` - Chat message records.

Write endpoints:

| Path | Schema | What it does |
|---|---|---|
| `/teams/{teamId}/channels/{channelId}/messages/new.json` | `/teams/{teamId}/channels/{channelId}/messages/new.schema.json` | Posts a new Teams channel message. |
| `/teams/{teamId}/channels/{channelId}/messages/{messageId}/replies/new.json` | `/teams/{teamId}/channels/{channelId}/messages/{messageId}/replies/new.schema.json` | Posts a reply to a Teams channel message. |
| `/teams/chats/{chatId}/messages/new.json` | `/teams/chats/{chatId}/messages/new.schema.json` | Posts a new Teams chat message. |

## Write field contracts

### Create Teams channel message

Path: `/teams/{teamId}/channels/{channelId}/messages/new.json`
Schema: `/teams/{teamId}/channels/{channelId}/messages/new.schema.json`
Example: `/teams/{teamId}/channels/{channelId}/messages/new.example.json`
Required fields: none at the top level.
Optional fields: `body`, `text`, `content`.
Validation: provide at least one of `body.content`, `text`, `content`.

Fields:

- `body` (optional, object) - Teams message body.
- `body.contentType` (optional, enum) - Message content type. The adapter sends html by default. Allowed values: `html`, `text`.
- `body.content` (required, string) - Message body content.
- `text` (optional, string) - Plain text or HTML message content. Used when `body.content` is omitted.
- `content` (optional, string) - Plain text or HTML message content. Used when `body.content` and `text` are omitted.

### Create Teams thread reply

Path: `/teams/{teamId}/channels/{channelId}/messages/{messageId}/replies/new.json`
Schema: `/teams/{teamId}/channels/{channelId}/messages/{messageId}/replies/new.schema.json`
Example: `/teams/{teamId}/channels/{channelId}/messages/{messageId}/replies/new.example.json`
Required fields: none at the top level.
Optional fields: `body`, `text`, `content`.
Validation: provide at least one of `body.content`, `text`, `content`.

Fields:

- `body` (optional, object) - Teams message body.
- `body.contentType` (optional, enum) - Message content type. The adapter sends html by default. Allowed values: `html`, `text`.
- `body.content` (required, string) - Message body content.
- `text` (optional, string) - Plain text or HTML message content. Used when `body.content` is omitted.
- `content` (optional, string) - Plain text or HTML message content. Used when `body.content` and `text` are omitted.

### Create Teams chat message

Path: `/teams/chats/{chatId}/messages/new.json`
Schema: `/teams/chats/{chatId}/messages/new.schema.json`
Example: `/teams/chats/{chatId}/messages/new.example.json`
Required fields: none at the top level.
Optional fields: `body`, `text`, `content`.
Validation: provide at least one of `body.content`, `text`, `content`.

Fields:

- `body` (optional, object) - Teams message body.
- `body.contentType` (optional, enum) - Message content type. The adapter sends html by default. Allowed values: `html`, `text`.
- `body.content` (required, string) - Message body content.
- `text` (optional, string) - Plain text or HTML message content. Used when `body.content` is omitted.
- `content` (optional, string) - Plain text or HTML message content. Used when `body.content` and `text` are omitted.

## How to use (agents)
1. Read the relevant `*.schema.json` to learn the required shape.
2. Optional: read the matching `new.example.json` for a starter document.
3. Write your document to the `new.json` path.
4. Read back the most-recently-created sibling record written by the adapter.
