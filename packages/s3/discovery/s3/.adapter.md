# Amazon S3 adapter

The S3 adapter exposes objects and SQS notification queues with file-native writeback discovery.

Read-only mounts:
- `/s3/<bucket>/<key>` - S3 object content.

Resources:

| Resource | Schema | Create example | ID pattern | What it does |
|---|---|---|---|---|
| `/s3/objects/<id>.json` | `/s3/objects/.schema.json` | `/s3/objects/.create.example.json` | `^[A-Za-z0-9_.:-]+$` | Uploads an S3 object. |
| `/s3/queues/<id>.json` | `/s3/queues/.schema.json` | `/s3/queues/.create.example.json` | `^[A-Za-z0-9_.:-]+$` | Creates an S3 queue notification config. |

## Operations

| To... | Do... |
|---|---|
| Read | `cat <canonical-file>.json` after listing the resource directory or following an alias when one is available. Canonical files may be bare ids or slug-prefixed names such as `<slug>__<id>.json`. |
| Edit | Write a partial JSON object to the canonical file. Only included mutable fields PATCH; fields marked `readOnly` in `.schema.json` are rejected. |
| Create | Write JSON to any non-canonical filename such as `create request.json`. The adapter creates the record at its canonical filename and rewrites the draft as `{ "created": "<real-id>", "path": "<resource>/<canonical-file>.json", "url": "<provider-url>" }`. |
| Ignore | Editor scratch files named `partial.json`, `.tmp.json`, `.partial.json`, `*.tmp.json`, or `*.partial.json` are ignored and never treated as create drafts. |
| Delete | `rm <canonical-file>.json` for canonical records. |

## ID Patterns
- `/s3/objects/<id>.json`: `^[A-Za-z0-9_.:-]+$`. Filenames that do not match this pattern are treated as create drafts.
- `/s3/queues/<id>.json`: `^[A-Za-z0-9_.:-]+$`. Filenames that do not match this pattern are treated as create drafts.

## Write field contracts

### Create S3 object

Resource: `/s3/objects/<id>.json`
Schema: `/s3/objects/.schema.json`
Create example: `/s3/objects/.create.example.json`
Required fields: `Bucket`, `Key`.
Optional fields: `BodyBase64`.

Fields:

- `Bucket` (required, string) - Bucket.
- `Key` (required, string) - Object key.
- `BodyBase64` (optional, string) - Base64 body.

### Create S3 queue notification

Resource: `/s3/queues/<id>.json`
Schema: `/s3/queues/.schema.json`
Create example: `/s3/queues/.create.example.json`
Required fields: `QueueUrl`.
Optional fields: `Bucket`.

Fields:

- `QueueUrl` (required, string) - SQS Queue URL.
- `Bucket` (optional, string) - Bucket.

## Create Examples
Read the resource `.schema.json` first, then use the sibling `.create.example.json` as a minimal create document. The example intentionally omits read-only fields.
