# @relayfile/adapter-docker-hub

Relayfile adapter package for the Docker Hub Composio bridge integration. It models repositories, tags, and webhooks from the Nango `docker_hub-composio-relay` syncs, including the Docker Hub layout manifest, path helpers, auxiliary index and alias emission, and digest classification consumed by AgentWorkforce Cloud once the package is published.
