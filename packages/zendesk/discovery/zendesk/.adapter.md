# Zendesk adapter

The Zendesk adapter exposes tickets, users, and organizations under `/zendesk`, with writeback routes for creating tickets, ticket comments, and users.

Read-only mounts:
- `/zendesk/tickets/<ticketId>.json` - Ticket records.
- `/zendesk/tickets/<ticketId>/comments/<commentId>.json` - Ticket comment records.
- `/zendesk/users/<userId>.json` - User records.
- `/zendesk/organizations/<organizationId>.json` - Organization records.

Resources:

| Resource | Schema | Create example | ID pattern | What it does |
|---|---|---|---|---|
| `/zendesk/tickets/{ticketId}/comments/<id>.json` | `/zendesk/tickets/{ticketId}/comments/.schema.json` | `/zendesk/tickets/{ticketId}/comments/.create.example.json` | `^\d+$` | Adds a comment to a Zendesk ticket. |
| `/zendesk/tickets/<id>.json` | `/zendesk/tickets/.schema.json` | `/zendesk/tickets/.create.example.json` | `^\d+$` | Creates a Zendesk ticket. |
| `/zendesk/users/<id>.json` | `/zendesk/users/.schema.json` | `/zendesk/users/.create.example.json` | `^\d+$` | Creates a Zendesk user. |

## Operations

| To... | Do... |
|---|---|
| Read | `cat <canonical-resource-path>` after listing the resource directory or following an alias when one is available. Use the resource table and ID patterns below to determine whether a resource uses a bare id, an adapter-specific slug/id filename, or an exact sidecar path such as `content.md`. |
| Edit | Write the resource update payload to the canonical resource path. For JSON resources, included mutable fields PATCH; fields marked `readOnly` in `.schema.json` are rejected. |
| Create | Write JSON to any non-canonical filename such as `create request.json`. The adapter creates the record at its canonical resource path and rewrites the draft as `{ "created": "<real-id>", "path": "<canonical-resource-path>", "url": "<provider-url>" }`. |
| Ignore | Editor scratch files named `partial.json`, `.tmp.json`, `.partial.json`, `*.tmp.json`, or `*.partial.json` are ignored and never treated as create drafts. |
| Delete | `rm <canonical-resource-path>` for canonical records. |

## ID Patterns
- `/zendesk/tickets/{ticketId}/comments/<id>.json`: `^\d+$`. Filenames that do not match this pattern are treated as create drafts.
- `/zendesk/tickets/<id>.json`: `^\d+$`. Filenames that do not match this pattern are treated as create drafts.
- `/zendesk/users/<id>.json`: `^\d+$`. Filenames that do not match this pattern are treated as create drafts.

## Write field contracts

### Create Zendesk ticket comment

Resource: `/zendesk/tickets/{ticketId}/comments/<id>.json`
Schema: `/zendesk/tickets/{ticketId}/comments/.schema.json`
Create example: `/zendesk/tickets/{ticketId}/comments/.create.example.json`
Required fields: `body`.
Optional fields: `html_body`, `public`.

Fields:

- `body` (required, string) - Plain-text comment body.
- `html_body` (optional, string) - HTML comment body.
- `public` (optional, boolean) - Whether the comment is public. Defaults to true.

### Create Zendesk ticket

Resource: `/zendesk/tickets/<id>.json`
Schema: `/zendesk/tickets/.schema.json`
Create example: `/zendesk/tickets/.create.example.json`
Required fields: `subject`.
Optional fields: `description`, `comment`, `requester_id`, `assignee_id`, `group_id`, `organization_id`, `priority`, `status`, `type`, `tags`, `custom_fields`.

Fields:

- `subject` (required, string) - Ticket subject.
- `description` (optional, string) - Ticket description.
- `comment` (optional, object) - Initial ticket comment.
- `requester_id` (optional, integer) - Requester user id.
- `assignee_id` (optional, integer) - Assignee user id.
- `group_id` (optional, integer) - Group id.
- `organization_id` (optional, integer) - Organization id.
- `priority` (optional, enum) - Ticket priority. Allowed values: `urgent`, `high`, `normal`, `low`.
- `status` (optional, enum) - Ticket status. Allowed values: `new`, `open`, `pending`, `hold`, `solved`, `closed`.
- `type` (optional, enum) - Ticket type. Allowed values: `problem`, `incident`, `question`, `task`.
- `tags` (optional, array) - Ticket tags.
- `custom_fields` (optional, array) - Zendesk custom fields.

### Create Zendesk user

Resource: `/zendesk/users/<id>.json`
Schema: `/zendesk/users/.schema.json`
Create example: `/zendesk/users/.create.example.json`
Required fields: `name`.
Optional fields: `email`, `role`, `phone`, `organization_id`, `external_id`, `tags`, `user_fields`, `verified`.

Fields:

- `name` (required, string) - User name.
- `email` (optional, string, email) - User email address.
- `role` (optional, string) - Zendesk user role.
- `phone` (optional, string) - User phone number.
- `organization_id` (optional, integer) - Organization id.
- `external_id` (optional, string) - External id.
- `tags` (optional, array) - User tags.
- `user_fields` (optional, object) - Zendesk user field values.
- `verified` (optional, boolean) - Whether the email is verified.

## Create Examples
Read the resource `.schema.json` first, then use the sibling `.create.example.json` as a minimal create document. The example intentionally omits read-only fields.
