# relayfile mount-root invariant incident

- timestamp: 20260714T151817Z
- local root: memory/workspace
- invariant: mount root must always be a directory
- detected kind: missing
- reason: local root does not exist

## Recovery

The mount root is gone. Confirm whether the directory was deleted
by another process (rm -rf, git clean -fdx, sync tool, etc.).
Recreate `memory/workspace`, then restart the mount using the same
workspace and connection settings. Remount or resync the workspace
to restore any provider-backed files that were removed with the root.
